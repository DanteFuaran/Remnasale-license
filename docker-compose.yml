services:
  license-bot:
    build: .
    container_name: remnasale-license
    restart: unless-stopped
    env_file: .env
    ports:
      - "9777:8080"
    volumes:
      - ./data:/data
      - /opt/remnasale-setup:/setup:ro
